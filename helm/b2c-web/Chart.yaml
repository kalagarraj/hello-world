apiVersion: v2
name: b2c-web
description: The NestJS web app that signs users in against Azure AD B2C
type: application

# Chart version: bumped when the templates change.
version: 0.1.0
# The application version this chart installs by default; image.tag overrides it.
appVersion: "0.1.0"

keywords: [nestjs, azure-ad-b2c, oidc]
home: https://github.com/kalagarraj/hello-world
