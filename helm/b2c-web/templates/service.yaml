apiVersion: v1
kind: Service
metadata:
  name: {{ include "b2c-web.fullname" . }}
  labels:
    {{- include "b2c-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "b2c-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
