{
  "name": "nestjs-b2c-hello-world",
  "version": "1.0.0",
  "description": "NestJS Hello World with Azure AD B2C authentication via openid-client (no MSAL)",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "redis:up": "docker compose up -d redis",
    "redis:down": "docker compose down",
    "redis:reset": "docker compose down -v",
    "redis:cli": "docker compose exec redis redis-cli",
    "redis:logs": "docker compose logs -f redis",
    "redis:sessions": "docker compose exec redis redis-cli --scan --pattern 'sess:*'",
    "cosmos:up": "docker compose -f docker-compose.cosmos.yml up -d",
    "cosmos:down": "docker compose -f docker-compose.cosmos.yml down",
    "cosmos:logs": "docker compose -f docker-compose.cosmos.yml logs -f"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/passport": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "connect-redis": "^10.0.0",
    "express-session": "^1.18.1",
    "hbs": "^4.2.0",
    "openid-client": "^5.7.1",
    "passport": "^0.7.0",
    "redis": "^6.2.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@types/express": "^5.0.0",
    "@types/express-session": "^1.18.1",
    "@types/node": "^22.10.0",
    "@types/passport": "^1.0.17",
    "typescript": "^5.7.0"
  }
}
