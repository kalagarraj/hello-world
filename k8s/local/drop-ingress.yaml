# No Ingress controller here, and the base Ingress carries placeholder hosts.
# Removing it is honest; leaving it would create an object that routes nowhere.
$patch: delete
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web
